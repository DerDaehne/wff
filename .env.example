# Kopieren nach .env und anpassen — von docker-compose.yml gelesen.

POSTGRES_USER=wff
POSTGRES_PASSWORD=wff
POSTGRES_DB=wff
POSTGRES_PORT=5432
APP_PORT=8080

# Optional — sinnvolle Defaults im Code, nur bei Bedarf überschreiben.
# ENRICHMENT_POLL_INTERVAL=1h
# OPENMETEO_BASE_URL=
