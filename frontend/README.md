# WFF Frontend (SvelteKit-PWA)

Installierbare Progressive Web App: Upload, Dashboard (Form/Load),
Ride-Liste, Ride-Detail (Karte + Kurven), Insights.

Noch nicht initialisiert. Erster Schritt (Epic #552):

```sh
cd frontend
pnpm create svelte@latest .   # SvelteKit-Grundgerüst
# danach PWA einrichten (Manifest + Service-Worker)
```

Zugehörige Epics: #552 (UI/UX & PWA), #549 (Karte, MapLibre-Komponente).
